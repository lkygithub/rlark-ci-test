# New Device Integration

Add new embodied hardware through `apps/embodied-runtime`: define a stable resource contract, implement discovery and health reporting, expose allocation through a device plugin or controller, and provide a minimal SDK example. Document host dependencies, permissions, supported versions, resource names, configuration, and cleanup. Test discovery, exclusive allocation, restart recovery, and failure reporting on real hardware.
