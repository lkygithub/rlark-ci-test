# Local Development

Build Go components with `make build` and start Server, Gateway, Controller Manager, and Agent with development kubeconfigs. For the Web UI, run `npm install` and `npm run dev` in `apps/rlark-ui`. The one-click kind and Docker Compose environment in the [Quick Start](../quickstart.md) is useful for integration testing, but is not a production deployment.
