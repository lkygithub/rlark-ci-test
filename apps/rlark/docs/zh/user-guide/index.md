# 平台使用指南

本指南假设管理员已经部署控制面，并至少纳管了一个数据面集群。平台用户可以在此基础上查看资源、提交任务、检查 Worker、使用 WebTerminal、创建工作流，以及管理存储和 SSH Key。

建议先查看[集群与节点](clusters-nodes.md)，再[创建任务](jobs.md)。

每篇指南统一采用两层说明：**通过 UI** 给出完成任务的最短页面路径；**API equivalent** 只标出对应能力并链接到参考手册。接口请求体和 curl 命令集中维护在 [API 示例](../api/examples.md)。
