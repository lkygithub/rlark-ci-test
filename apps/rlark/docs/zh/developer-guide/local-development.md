# 本地源码运行

使用 `make build` 构建 Go 组件，并以开发 kubeconfig 启动 Server、Gateway、Controller Manager 和 Agent。Web UI 在 `apps/rlark-ui` 中执行 `npm install` 与 `npm run dev`。 [快速开始](../quickstart.md)提供的 kind 与 Docker Compose 一键环境适合集成测试，但不属于生产部署方案。
