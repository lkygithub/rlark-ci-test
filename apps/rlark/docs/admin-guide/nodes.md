# Node and Resource Management

Only usable Worker nodes should contribute to platform capacity. Administrators can cordon or uncordon nodes and maintain RLark metadata such as city, node category, GPU model, and embodied-device model. Use batch changes carefully: update only explicitly selected fields and verify the resulting control-plane resource before scheduling production workloads.
