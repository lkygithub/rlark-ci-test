# Embodied Runtime Deployment

Deploy the Embodied Runtime device plugin and the required robot or camera controllers only on compatible edge clusters. Confirm host runtime dependencies, device access, resource names, node selectors, and controller configuration before advertising a device to RLark. Validate discovery and allocation with a non-production device first.
