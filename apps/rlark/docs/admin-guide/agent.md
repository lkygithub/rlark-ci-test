# Agent Installation and Upgrade

Install the cluster Agent from the command generated by the administrator console. Keep the control-plane address, certificates, cluster identity, namespace, RBAC, and image version consistent. Before upgrading, review release notes, back up manifests, upgrade one non-critical cluster first, and verify heartbeat, resource synchronization, workload creation, logs, and terminal access before continuing.
