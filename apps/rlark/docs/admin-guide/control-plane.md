# Production Control Plane

The production control plane consists of kcp, Server, Gateway, Controller Manager, database, and Web UI services. Use the Kubernetes installation flow and production configuration described in the [Deployment Guide](../deployment.md). Replace example credentials, terminate TLS with trusted certificates, restrict Gateway access, configure persistent database storage, and establish backup and upgrade procedures before onboarding production clusters.
