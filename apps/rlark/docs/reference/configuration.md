# Configuration Reference

Configuration is split across control-plane services, data-plane Agents, cluster registration, addons, and workload resources. The [Deployment Guide](../deployment.md) documents component flags and manifests; example files live in `docs/examples`. Never reuse example passwords, tokens, private keys, or certificates in production.
