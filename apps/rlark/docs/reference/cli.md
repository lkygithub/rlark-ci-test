# CLI Reference

RLark ships `rlarkadm` for installation and lifecycle operations and `rlarkctl` for platform resource operations. Run each command with `--help` to inspect flags supported by the installed version. Installation examples are maintained in the [Deployment Guide](../deployment.md); command output and flags may change between releases.
